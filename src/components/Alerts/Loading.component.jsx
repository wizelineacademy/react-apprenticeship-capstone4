import { Alert, LoadingContainer } from './themes/loading.style';

function Loading() {
  return (
    <LoadingContainer>
      <Alert>Loading</Alert>
    </LoadingContainer>
  );
}

export default Loading;
